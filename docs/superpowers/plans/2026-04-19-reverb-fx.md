# Reverb FX Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a `juce::Reverb`-backed Reverb effect (Room, Damp, Mix) wired into FXChain and exposed on the FX tab.

**Architecture:** New `Reverb` class follows the Delay/Chorus pattern (prepare/setParams/process/reset). `FXChain` adds it as the last processor in the chain. Three new APVTS parameters. `FxTab` gains a third equal-width panel.

**Tech Stack:** C++17, JUCE 7.0.9 (`juce::Reverb`, `juce_dsp` already linked), APVTS, CMake / Visual Studio 2022.

---

## File Map

| Action | File | Responsibility |
|---|---|---|
| Modify | `Source/Parameters/ParameterIDs.h` | 3 new ID constants |
| Modify | `Source/Parameters/ParameterLayout.cpp` | Register 3 new APVTS params |
| Create | `Source/FX/Reverb.h` | Class declaration |
| Create | `Source/FX/Reverb.cpp` | Implementation via `juce::Reverb` |
| Modify | `CMakeLists.txt` | Add `Reverb.cpp` to source list |
| Modify | `Source/FX/FXChain.h` | Add `Reverb` member, extend `setParams` signature |
| Modify | `Source/FX/FXChain.cpp` | Forward calls to reverb |
| Modify | `Source/PluginProcessor.h` | Add `buildReverbParams()` declaration |
| Modify | `Source/PluginProcessor.cpp` | Implement `buildReverbParams()`, pass to `fxChain.setParams` |
| Modify | `Source/UI/FXTab.h` | Add REVERB panel with 3 knobs |

---

## Task 1: Add parameter ID constants

**Files:**
- Modify: `Source/Parameters/ParameterIDs.h`

- [ ] **Step 1: Add 3 new IDs after the Chorus block**

In `Source/Parameters/ParameterIDs.h`, add inside the `ParamIDs` namespace, after `chorusMix` and before `unisonVoices`:

```cpp
    // Reverb
    inline constexpr const char* reverbRoom = "reverb_room";
    inline constexpr const char* reverbDamp = "reverb_damp";
    inline constexpr const char* reverbMix  = "reverb_mix";
```

- [ ] **Step 2: Commit**

```bash
git add Source/Parameters/ParameterIDs.h
git commit -m "feat: add reverb parameter ID constants"
```

---

## Task 2: Register APVTS parameters

**Files:**
- Modify: `Source/Parameters/ParameterLayout.cpp`

- [ ] **Step 1: Add reverb params after the Chorus block**

In `Source/Parameters/ParameterLayout.cpp`, add after the `chorusMix` push_back and before the Unison block:

```cpp
    // -----------------------------------------------------------------------
    // Reverb
    // -----------------------------------------------------------------------
    params.push_back(std::make_unique<AudioParameterFloat>(
        ParamIDs::reverbRoom, "Reverb Room",
        NormalisableRange<float>(0.f, 1.f, 0.01f), 0.5f));
    params.push_back(std::make_unique<AudioParameterFloat>(
        ParamIDs::reverbDamp, "Reverb Damp",
        NormalisableRange<float>(0.f, 1.f, 0.01f), 0.5f));
    params.push_back(std::make_unique<AudioParameterFloat>(
        ParamIDs::reverbMix, "Reverb Mix",
        NormalisableRange<float>(0.f, 1.f, 0.001f), 0.f));
```

- [ ] **Step 2: Build to verify no compile errors**

```bat
build.bat
```
Expected: build completes with no errors.

- [ ] **Step 3: Commit**

```bash
git add Source/Parameters/ParameterLayout.cpp
git commit -m "feat: register reverb APVTS parameters"
```

---

## Task 3: Create Reverb class

**Files:**
- Create: `Source/FX/Reverb.h`
- Create: `Source/FX/Reverb.cpp`
- Modify: `CMakeLists.txt`

- [ ] **Step 1: Write `Reverb.h`**

Create `Source/FX/Reverb.h`:

```cpp
#pragma once
#include <juce_dsp/juce_dsp.h>
#include <juce_audio_basics/juce_audio_basics.h>

class Reverb
{
public:
    struct Params
    {
        float room = 0.5f;  // 0–1
        float damp = 0.5f;  // 0–1
        float mix  = 0.f;   // 0 = dry, 1 = wet
    };

    void prepare(double sampleRate, int maxBlockSize);
    void setParams(const Params& p);
    void process(juce::AudioBuffer<float>& buffer);
    void reset();

private:
    juce::Reverb reverb;
    Params params;
};
```

- [ ] **Step 2: Write `Reverb.cpp`**

Create `Source/FX/Reverb.cpp`:

```cpp
#include "Reverb.h"

void Reverb::prepare(double sampleRate, int /*maxBlockSize*/)
{
    reverb.setSampleRate(sampleRate);
    reverb.reset();
}

void Reverb::setParams(const Params& p)
{
    params = p;
    juce::Reverb::Parameters rp;
    rp.roomSize   = p.room;
    rp.damping    = p.damp;
    rp.wetLevel   = p.mix;
    rp.dryLevel   = 1.f - p.mix;
    rp.width      = 1.f;
    rp.freezeMode = 0.f;
    reverb.setParameters(rp);
}

void Reverb::process(juce::AudioBuffer<float>& buffer)
{
    if (params.mix < 0.001f)
        return;

    const int numSamples = buffer.getNumSamples();
    if (buffer.getNumChannels() >= 2)
        reverb.processStereo(buffer.getWritePointer(0),
                             buffer.getWritePointer(1),
                             numSamples);
    else
        reverb.processMono(buffer.getWritePointer(0), numSamples);
}

void Reverb::reset()
{
    reverb.reset();
}
```

- [ ] **Step 3: Register `Reverb.cpp` in `CMakeLists.txt`**

In `CMakeLists.txt`, add `Source/FX/Reverb.cpp` right after the `Source/FX/Chorus.cpp` line:

```cmake
    Source/FX/Chorus.cpp
    Source/FX/Reverb.cpp
```

- [ ] **Step 4: Build to verify**

```bat
build.bat
```
Expected: build completes with no errors. If you see "juce::Reverb not found", check that `juce_dsp` is in the target's `target_link_libraries` in CMakeLists.txt (it already is in this project).

- [ ] **Step 5: Commit**

```bash
git add Source/FX/Reverb.h Source/FX/Reverb.cpp CMakeLists.txt
git commit -m "feat: add Reverb class wrapping juce::Reverb"
```

---

## Task 4: Extend FXChain

**Files:**
- Modify: `Source/FX/FXChain.h`
- Modify: `Source/FX/FXChain.cpp`

- [ ] **Step 1: Update `FXChain.h`**

Replace the entire content of `Source/FX/FXChain.h`:

```cpp
#pragma once
#include "Delay.h"
#include "Chorus.h"
#include "Reverb.h"

class FXChain
{
public:
    void prepare(double sampleRate, int maxBlockSize);
    void setParams(const Delay::Params& dp,
                   const Chorus::Params& cp,
                   const Reverb::Params& rp);
    void process(juce::AudioBuffer<float>& buffer);
    void reset();

private:
    Delay  delay;
    Chorus chorus;
    Reverb reverb;
};
```

- [ ] **Step 2: Update `FXChain.cpp`**

Replace the entire content of `Source/FX/FXChain.cpp`:

```cpp
#include "FXChain.h"

void FXChain::prepare(double sr, int maxBlockSize)
{
    delay.prepare(sr, maxBlockSize);
    chorus.prepare(sr, maxBlockSize);
    reverb.prepare(sr, maxBlockSize);
}

void FXChain::setParams(const Delay::Params& dp,
                        const Chorus::Params& cp,
                        const Reverb::Params& rp)
{
    delay.setParams(dp);
    chorus.setParams(cp);
    reverb.setParams(rp);
}

void FXChain::process(juce::AudioBuffer<float>& buffer)
{
    delay.process(buffer);
    chorus.process(buffer);
    reverb.process(buffer);
}

void FXChain::reset()
{
    delay.reset();
    chorus.reset();
    reverb.reset();
}
```

- [ ] **Step 3: Stage but do NOT build or commit yet**

The `fxChain.setParams(...)` call in `PluginProcessor.cpp` still passes 2 arguments; the build will fail until Task 5 updates that call site. Stage the files and proceed directly to Task 5.

```bash
git add Source/FX/FXChain.h Source/FX/FXChain.cpp
```

---

## Task 5: Wire Reverb into PluginProcessor

**Files:**
- Modify: `Source/PluginProcessor.h`
- Modify: `Source/PluginProcessor.cpp`

- [ ] **Step 1: Add `buildReverbParams()` declaration to `PluginProcessor.h`**

In `Source/PluginProcessor.h`, in the `private:` section, add after `buildChorusParams()`:

```cpp
    Reverb::Params    buildReverbParams() const;
```

The private section should now read:

```cpp
    VoiceParams       buildVoiceParams()  const;
    Delay::Params     buildDelayParams()  const;
    Chorus::Params    buildChorusParams() const;
    Reverb::Params    buildReverbParams() const;
    ArpEngine::Params buildArpParams()    const;
```

- [ ] **Step 2: Add `buildReverbParams()` implementation to `PluginProcessor.cpp`**

In `Source/PluginProcessor.cpp`, add the new function after `buildChorusParams()`:

```cpp
Reverb::Params AISynthProcessor::buildReverbParams() const
{
    Reverb::Params p;
    p.room = *apvts.getRawParameterValue(ParamIDs::reverbRoom);
    p.damp = *apvts.getRawParameterValue(ParamIDs::reverbDamp);
    p.mix  = *apvts.getRawParameterValue(ParamIDs::reverbMix);
    return p;
}
```

- [ ] **Step 3: Update `fxChain.setParams` call in `processBlock`**

In `PluginProcessor.cpp`, in `processBlock()`, replace:

```cpp
    fxChain.setParams(buildDelayParams(), buildChorusParams());
```

With:

```cpp
    fxChain.setParams(buildDelayParams(), buildChorusParams(), buildReverbParams());
```

- [ ] **Step 4: Add `#include "FX/Reverb.h"` if needed**

`PluginProcessor.h` already includes `FX/FXChain.h` which now includes `Reverb.h`, so no additional include is needed.

- [ ] **Step 5: Build to verify (FXChain + PluginProcessor together)**

```bat
build.bat
```
Expected: build completes with no errors. VST3 at `build\AISynth_artefacts\Release\VST3\AI Synth.vst3`.

- [ ] **Step 6: Commit FXChain + PluginProcessor together**

```bash
git add Source/FX/FXChain.h Source/FX/FXChain.cpp Source/PluginProcessor.h Source/PluginProcessor.cpp
git commit -m "feat: extend FXChain with Reverb, wire buildReverbParams into processBlock"
```

---

## Task 6: Add Reverb UI panel in FXTab

**Files:**
- Modify: `Source/UI/FXTab.h`

- [ ] **Step 1: Replace the entire content of `Source/UI/FXTab.h`**

```cpp
#pragma once
#include <juce_audio_processors/juce_audio_processors.h>
#include "KnobWithLabel.h"
#include "LookAndFeel.h"
#include "../Parameters/ParameterIDs.h"

class FxTab : public juce::Component
{
public:
    explicit FxTab(juce::AudioProcessorValueTreeState& apvts) : apvts(apvts)
    {
        addAndMakeVisible(knobDelayTime);
        addAndMakeVisible(knobDelayFeedback);
        addAndMakeVisible(knobDelayMix);
        addAndMakeVisible(knobChorusRate);
        addAndMakeVisible(knobChorusDepth);
        addAndMakeVisible(knobChorusMix);
        addAndMakeVisible(knobReverbRoom);
        addAndMakeVisible(knobReverbDamp);
        addAndMakeVisible(knobReverbMix);
    }

    void paint(juce::Graphics& g) override
    {
        drawPanel(g, delayBounds,  "DELAY",  SynthLookAndFeel::colFxAccent);
        drawPanel(g, chorusBounds, "CHORUS", SynthLookAndFeel::colFxAccent);
        drawPanel(g, reverbBounds, "REVERB", SynthLookAndFeel::colFxAccent);
    }

    void resized() override
    {
        const int pad    = 8;
        const int titleH = 20;
        auto area = getLocalBounds().reduced(pad);
        const int colW = area.getWidth() / 3;
        delayBounds  = area.removeFromLeft(colW).reduced(pad, 0);
        chorusBounds = area.removeFromLeft(colW).reduced(pad, 0);
        reverbBounds = area.reduced(pad, 0);

        auto layoutFx = [&](juce::Rectangle<int> bounds,
                            KnobWithLabel& k1, KnobWithLabel& k2, KnobWithLabel& k3)
        {
            auto b = bounds.withTrimmedTop(titleH);
            const int w = b.getWidth() / 3;
            k1.setBounds(b.removeFromLeft(w));
            k2.setBounds(b.removeFromLeft(w));
            k3.setBounds(b);
        };

        layoutFx(delayBounds,  knobDelayTime,   knobDelayFeedback, knobDelayMix);
        layoutFx(chorusBounds, knobChorusRate,  knobChorusDepth,   knobChorusMix);
        layoutFx(reverbBounds, knobReverbRoom,  knobReverbDamp,    knobReverbMix);
    }

private:
    juce::AudioProcessorValueTreeState& apvts;
    juce::Rectangle<int> delayBounds, chorusBounds, reverbBounds;

    KnobWithLabel knobDelayTime     { "Time",     apvts, ParamIDs::delayTime,     " ms", 0 };
    KnobWithLabel knobDelayFeedback { "Feedback", apvts, ParamIDs::delayFeedback, "",    2 };
    KnobWithLabel knobDelayMix      { "Mix",      apvts, ParamIDs::delayMix,      "",    2 };
    KnobWithLabel knobChorusRate    { "Rate",     apvts, ParamIDs::chorusRate,    " Hz", 2 };
    KnobWithLabel knobChorusDepth   { "Depth",    apvts, ParamIDs::chorusDepth,   "",    3 };
    KnobWithLabel knobChorusMix     { "Mix",      apvts, ParamIDs::chorusMix,     "",    2 };
    KnobWithLabel knobReverbRoom    { "Room",     apvts, ParamIDs::reverbRoom,    "",    2 };
    KnobWithLabel knobReverbDamp    { "Damp",     apvts, ParamIDs::reverbDamp,    "",    2 };
    KnobWithLabel knobReverbMix     { "Mix",      apvts, ParamIDs::reverbMix,     "",    2 };

    static void drawPanel(juce::Graphics& g, juce::Rectangle<int> bounds,
                          const juce::String& title, juce::uint32 accentColour)
    {
        auto f = bounds.toFloat().reduced(2.f);
        g.setColour(juce::Colour(SynthLookAndFeel::colPanel));
        g.fillRoundedRectangle(f, 6.f);
        g.setColour(juce::Colour(accentColour));
        g.drawRoundedRectangle(f, 6.f, 1.f);
        g.setColour(juce::Colour(SynthLookAndFeel::colHighlight));
        g.setFont(juce::Font(11.f, juce::Font::bold));
        g.drawText(title, bounds.getX() + 10, bounds.getY() + 4,
                   bounds.getWidth() - 20, 16, juce::Justification::centredLeft);
    }
};
```

- [ ] **Step 2: Build to verify**

```bat
build.bat
```
Expected: build completes with no errors.

- [ ] **Step 3: Commit**

```bash
git add Source/UI/FXTab.h
git commit -m "feat: add REVERB panel with Room/Damp/Mix knobs to FXTab"
```

---

## Task 7: Smoke test

- [ ] **Step 1: Deploy the plugin**

```bat
deploy.bat
```
Expected: `AI Synth.vst3` copied to `C:\Program Files\Common Files\VST3\`.

- [ ] **Step 2: Test in plugin host**

Load the plugin in a DAW or JUCE AudioPluginHost. Verify:

1. **Default (mix = 0)**: Sound is identical to before — no CPU overhead from reverb.
2. **Mix up, Room 0.5**: Audible short reverb tail on held notes.
3. **Room = 1.0**: Long, large-hall reverb tail.
4. **Damp = 1.0**: Reverb tail sounds muffled/dark (high frequencies attenuated).
5. **Damp = 0.0**: Reverb tail sounds bright.
6. **Stereo**: Reverb output is properly stereo (rotate pan on the track to confirm both channels have reverb).
7. **No regression**: Delay and Chorus still work correctly on the FX tab.
8. **FX tab layout**: Three equal panels DELAY / CHORUS / REVERB visible side-by-side.
